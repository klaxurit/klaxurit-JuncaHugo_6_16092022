{% block stylesheets %}<link rel="stylesheet" href="{{ asset('assets/css/_nav.css') }}">{% endblock %}
<!-- Navigation-->
<nav class="navbar navbar-expand-sm navbar-light primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">SnowTricks</a>
        <button class="navbar-toggler bg-primary border-primary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Home</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Log-out</a>
                </li>	
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Sign-in</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Log-in</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_comments') }}">Admin</a>
                </li>				
                {% endif %}
            </ul>		  
        </div>
    </div>
</nav>
